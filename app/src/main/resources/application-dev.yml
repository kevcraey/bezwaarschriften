bezwaarschriften:
  input:
    folder: input
  testdata:
    pad: testdata
  extractie:
    max-concurrent: 3
    max-pogingen: 2
  clustering:
    min-cluster-size: 5
    min-samples: 3
    cluster-selection-epsilon: 0.2
    umap-enabled: true
    umap-n-components: 5
    umap-n-neighbors: 15
    umap-min-dist: 0.1
  embedding:
    provider: ollama
    model: bge-m3
    dimensions: 1024
    ollama-url: http://localhost:11434

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bezwaarschriften
    username: bezwaarschriften_user
    password: bezwaarschriften_password
  autoconfigure:
    # prevent lookup of keycloak URL on Bamboo
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
      - org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration
  cloud:
    service-registry:
      auto-registration:
        enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false
  security:
    oauth2:
      client:
        registration:
          omgeving:
            client-secret: '{noop}secret'

management:
  health:
    elasticsearch:
      enabled: false

kc:
  realm: Omgeving
  base-url: https://ssov2-ontwikkel.omgeving.vlaanderen.be/auth
  internal-base-url: http://keycloak-on.lb.cumuli.be:8080/auth


eureka:
  client:
    enabled: false

cumuli:
  security:
    enable:
      mock: true
    users:
      - username: bezwaarschriftenSpringBootAdmin
        password: "{noop}bezwaarschriftenSpringBootAdmin"
        roles:
          - BezwaarschriftenSpringBootAdmin
      - username: gebruiker
        password: "{noop}gebruiker"
        roles:
          - BezwaarschriftenGebruiker
    systemuser:
      name: bezwaarschriften
      password: "{noop}bezwaarschriften"

logging:
  file:
    path: "./target"
