import { BaseHTMLElement } from '@domg-wc/common';
import './vl-cookie-consent-opt-in.component';
export declare class VlCookieConsent extends BaseHTMLElement {
    private initialized;
    private _cookieConsentCookieName;
    private _optIns;
    private _cookieConsentDateCookieName;
    private _cookieConsentResetDate;
    constructor();
    static get _observedAttributes(): string[];
    get _isAutoOpenDisabled(): boolean;
    get _isFunctionalOptInDisabled(): boolean;
    get _cookiePrefix(): string;
    get _modalElement(): any;
    get _formGridElement(): any;
    get _optInElementen(): NodeListOf<import("./vl-cookie-consent-opt-in.component").VlCookieConsentOptIn>;
    get _ownerElements(): NodeListOf<HTMLElement> | undefined;
    get _linkElement(): HTMLAnchorElement | null | undefined;
    connectedCallback(): void;
    disconnectedCallback(): void;
    /**
     * Opent de cookie-consent ook al werd deze eerder getoond aan de gebruiker.
     * @return {void}
     */
    open(): void;
    /**
     * Sluit de cookie-consent.
     * @return {void}
     */
    close(): void;
    /**
     * Verwijdert al de cookies en herstelt de opt-in waarden naar de initiële toestand.
     * @return {void}
     */
    reset(): void;
    /**
     * Voeg een opt-in toe.
     * @param {object} optIn - De opt-in optie met attributen.
     * @param {string} optIn.name - De naam van de opt-in optie.
     * @param {string} optIn.label - Het label van de opt-in optie die getoond zal worden aan de gebruiker.
     * @param {string} optIn.description - De beschrijving van de opt-in optie die getoond zal worden aan de gebruiker.
     * @param {boolean} optIn.value - De standaard opt-in optie waarde die bepaalt of de opt-in standaard geactiveerd wordt.
     * @param {boolean} optIn.mandatory - Indien de opt-in verplicht is, zal de opt-in standaard geactiveerd worden en kan deze niet gewijzigd worden door de gebruiker.
     * @param {function} optIn.callback - De callback functies.
     * @param {function} optIn.callback.activated - Functie die aangeroepen wordt wanneer de gebruiker de cookie-consent bevestigt en de opt-in geactiveerd werd.
     * @param {function} optIn.callback.deactivated - Functie die aangeroepen wordt wanneer de gebruiker de cookie-consent bevestigt en de opt-in gedactiveerd werd.
     * @return {void}
     */
    addOptIn(optIn: any): void;
    /**
     * Voegt aan een opt-in een callback toe die aangeroepen wordt wanneer de opt-in geactiveerd wordt.
     * @param {string} name - De opt-in optie naam.
     * @param {function} callback - Callback functie.
     */
    addOptInActivatedCallback(name: string, callback: any): void;
    /**
     * Voegt aan een opt-in een callback toe die aangeroepen wordt wanneer de opt-in gedeactiveerd wordt.
     * @param {string} name - De opt-in optie naam.
     * @param {function} callback - Callback functie.
     */
    addOptInDeactivatedCallback(name: string, callback: any): void;
    /**
     * Bepaalt of een opt-in actief is of niet op basis van de naam.
     * @param {string} name - De opt-in optie naam.
     * @return {boolean}
     */
    isOptInActive(name: string): boolean;
    /**
     * Geeft terug of de cookie-consent modal geopend is.
     * @return {boolean}
     */
    isOpen(): any;
    _getButtonTemplate(): DocumentFragment;
    _getOptInTemplate(optIn: any): DocumentFragment | undefined;
    _open(forced: boolean): void;
    _resetOptInValue(optIn: any): void;
    _processOptIns(): void;
    _processOptIn({ name, label, description, value, mandatory, callback: { activated, deactivated }, }: any): void;
    _submitOptIns(): void;
    _bevatOptIn(name: string): boolean;
    _addFunctionalOptIn(): void;
    _addAnalytics(): void;
    _getCookie(name: string): any;
    _getCookieConsentCookie(): any;
    _getCookieConsentDateCookie(): any;
    _setCookie(name: string, value: any): void;
    _deleteCookie(name: string): void;
    _heeftCookieConsentDateCookie(): boolean;
    _isCookieConsentCookieGeldig(): boolean;
    _analyticsChangedCallback(oldValue: any, newValue: any): void;
    __addAnalyticsIfOptedIn(): void;
    _ownerChangedCallback(oldValue: any, newValue: any): void;
    _linkChangedCallback(oldValue: string, newValue: string): void;
    _dispatchCloseEvent: () => void;
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-cookie-consent': VlCookieConsent;
    }
}
