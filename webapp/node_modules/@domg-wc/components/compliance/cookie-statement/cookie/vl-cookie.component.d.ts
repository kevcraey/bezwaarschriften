import { BaseHTMLElement } from '@domg-wc/common';
export interface VlCookieProps {
    title?: string;
    name?: string | string[];
    purpose?: string;
    domain?: string;
    processor?: string;
    validity?: string;
}
export declare class VlCookie extends BaseHTMLElement {
    constructor({ title, name, purpose, domain, processor, validity }?: VlCookieProps);
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-cookie': VlCookie;
    }
}
