import { VlCookie } from './vl-cookie.component';
export declare class VlJSessionIdCookie extends VlCookie {
    constructor();
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-jsessionid-cookie': VlJSessionIdCookie;
    }
}
