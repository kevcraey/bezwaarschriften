import { VlCookie } from './vl-cookie.component';
export declare class VlHeaderCookie extends VlCookie {
    constructor();
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-header-cookie': VlHeaderCookie;
    }
}
