import { VlCookie } from './vl-cookie.component';
export declare class VlStickySessionCookie extends VlCookie {
    constructor();
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-sticky-session-cookie': VlStickySessionCookie;
    }
}
