import { VlCookie } from './vl-cookie.component';
export declare class VlAuthenticationCookie extends VlCookie {
    constructor();
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-authentication-cookie': VlAuthenticationCookie;
    }
}
