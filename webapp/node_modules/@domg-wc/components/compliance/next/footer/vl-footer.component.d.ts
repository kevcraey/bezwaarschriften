import { BaseLitElement } from '@domg-wc/common';
import { GlobalFooterClient } from '@govflanders/vl-widget-global-footer-types';
declare global {
    interface Window {
        globalFooterClient: GlobalFooterClient;
    }
}
export declare class VlFooterNext extends BaseLitElement {
    private development;
    private identifier;
    static get properties(): {
        development: {
            type: BooleanConstructor;
            attribute: string;
            reflect: boolean;
        };
        identifier: {
            type: StringConstructor;
            attribute: string;
            reflect: boolean;
        };
    };
    constructor();
    get footerContainer(): Element | null;
    private injectFooterContainer;
    private onReady;
    private loadWidget;
    render(): void;
    createRenderRoot(): this;
    disconnectedCallback(): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-footer-next': VlFooterNext;
    }
}
