import { BaseLitElement } from '@domg-wc/common';
import { ApplicationMenuLink, GlobalHeaderClient } from '@govflanders/vl-widget-global-header-types';
import { PropertyDeclarations } from 'lit';
export type ApplicationLink = ApplicationMenuLink;
declare global {
    interface Window {
        globalHeaderClient: GlobalHeaderClient;
    }
}
export declare class VlHeaderNext extends BaseLitElement {
    applicationLinks: ApplicationMenuLink[];
    private authenticatedUserUrl;
    private development;
    private identifier;
    private loginUrl;
    private logoutUrl;
    private switchCapacityUrl;
    private simple;
    private skeleton;
    constructor();
    static get properties(): PropertyDeclarations;
    private get headerContainer();
    private get headerContainerSkeleton();
    connectedCallback(): void;
    disconnectedCallback(): void;
    protected willUpdate(changedProperties: Map<string, unknown>): void;
    protected createRenderRoot(): HTMLElement | DocumentFragment;
    private injectHeaderContainer;
    private injectHeaderContainerSkeleton;
    private isUserAuthenticated;
    private onReady;
    private loadWidget;
    private configureSession;
    get height(): number;
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-header-next': VlHeaderNext;
    }
}
