import { BaseLitElement } from '@domg-wc/common';
import { CSSResult, PropertyDeclarations, TemplateResult } from 'lit';
import { VlAlertModel } from './vl-alert.model';
export declare class VlAlert extends BaseLitElement implements VlAlertModel {
    icon: string;
    title: string;
    type?: 'info' | 'success' | 'warning' | 'error';
    size: string;
    message: string;
    naked: boolean;
    closable: boolean;
    static get styles(): CSSResult[];
    static get properties(): PropertyDeclarations;
    protected updated(changedProperties: Map<string, unknown>): void;
    protected render(): TemplateResult;
    private removeAlert;
    private processButtons;
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-alert': VlAlert;
    }
}
