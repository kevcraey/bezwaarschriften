import { PropertyDeclarations } from 'lit';
import { MaskOptions } from '../models/cleave.model';
import { VlInputFieldComponent } from '../input-field';
export declare class VlInputFieldMaskedComponent extends VlInputFieldComponent {
    private mask;
    private maskPrefix;
    private rawValue;
    private disableMaskValidation;
    private static masks;
    private maskOptions;
    private cleaveInstance;
    static formControlValidators: import("@open-wc/form-control").Validator[];
    static get properties(): PropertyDeclarations;
    static setMasks(masks: Record<string, MaskOptions>): void;
    static getMask(key: string): MaskOptions | undefined;
    connectedCallback(): void;
    firstUpdated(changedProperties: Map<string, unknown>): void;
    resetFormControl(): void;
    getRawValue(): string | undefined;
    protected onInput(): void;
    protected onUpdated(changedProperties: Map<string, unknown>): void;
    private handleValueChanged;
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-input-field-masked': VlInputFieldMaskedComponent;
    }
}
