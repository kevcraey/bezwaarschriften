import { html } from 'lit';
export const buttonVariants = ['primary', 'secondary', 'tertiary', 'ghost', 'error', 'toggle'];
export const buttonSizes = ['default', 'large', 'wide', 'narrow', 'block'];
export const buttonIcons = ['none', 'before', 'after', 'only'];
export const buttonStates = ['idle', 'loading', 'disabled'];
const buttonClasses = (buttonVariant, buttonSize, buttonIcon, buttonState) => `${buttonVariant} ${buttonSize} icon-${buttonIcon} ${buttonState}`;
const buttonTitle = (element, buttonVariant, buttonSize, buttonIcon, buttonState) => html `<dl style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
        <dt><strong>Element</strong></dt>
        <dd>${element}</dd>
        <dt><strong>Variant</strong></dt>
        <dd>${buttonVariant}</dd>
        <dt><strong>Size</strong></dt>
        <dd>${buttonSize}</dd>
        <dt><strong>Icon</strong></dt>
        <dd>${buttonIcon}</dd>
        <dt><strong>State</strong></dt>
        <dd>${buttonState}</dd>
    </dl>`;
const buttonContent = (buttonIcon) => html `${buttonIcon === 'before'
    ? html `<span class="vl-icon vl-icon--before vl-icon--add"></span>`
    : ''}
${buttonIcon === 'only' ? html `<span class="vl-icon vl-icon--add"></span>` : 'Klik hier'}
${buttonIcon === 'after' ? html `<span class="vl-icon vl-icon--after vl-icon--add"></span>` : ''} `;
export const renderAllButtonVariants = (element, selector) => html `<div
    style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem;"
>
    ${buttonVariants.map((buttonVariant) => buttonSizes.map((buttonSize) => buttonIcons.map((buttonIcon) => buttonStates.map((buttonState) => html `
                            <div>
                                ${element === 'button'
    ? html `<button
                                          id="${[buttonVariant, buttonSize, buttonIcon, buttonState].join('_')}"
                                          class="${selector} ${buttonClasses(buttonVariant, buttonSize, buttonIcon, buttonState)}"
                                      >
                                          ${buttonContent(buttonIcon)}
                                      </button>`
    : html `<a
                                          id="${[buttonVariant, buttonSize, buttonIcon, buttonState].join('_')}"
                                          href="#"
                                          class="${selector} ${buttonClasses(buttonVariant, buttonSize, buttonIcon, buttonState)}"
                                      >
                                          ${buttonContent(buttonIcon)}
                                      </a>`}
                                <vl-popover
                                    for="${[buttonVariant, buttonSize, buttonIcon, buttonState].join('_')}"
                                    trigger="focus"
                                >
                                    ${buttonTitle(element, buttonVariant, buttonSize, buttonIcon, buttonState)}
                                </vl-popover>
                            </div>
                        `))))}
</div>`;
//# sourceMappingURL=vl-button-style.util.js.map