import { CSSResult } from 'lit';
export declare class GlobalStyles {
    private static instance;
    private defaultStyles;
    private registered;
    private customCss;
    private registeredCustomCss;
    private constructor();
    static getInstance(defaultStyles?: CSSResult[]): GlobalStyles;
    register(): void;
    addCustomCss(customCSS: CSSResult[]): void;
    private registerCustomCSS;
}
