import { VlSelectAction } from './select-action';
export declare class VlShowInfoSelectAction extends VlSelectAction {
    private tooltips;
    private visible;
    constructor(layer: any, infoPromise: any, loadingMessage: any, doneLoading: any, tooltipOptions?: any);
    clear(): void;
    deactivate(): void;
    activate(): void;
}
