import { VlDrawAction } from '../draw/draw-action';
export declare class VlShowInfoAction extends VlDrawAction {
    private Vltooltips;
    constructor(layer: any, infoPromise: any, loadingMessage: any, tooltipOptions?: any);
    clear(): void;
    deactivate(): void;
}
