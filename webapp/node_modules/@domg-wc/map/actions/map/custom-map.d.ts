import Collection from 'ol/Collection';
import BaseLayer from 'ol/layer/Base';
import LayerGroup from 'ol/layer/Group';
import { MapOptions } from 'ol/Map';
import { Projection } from 'ol/proj';
import { VlMapWithActions } from './map-with-actions';
/**
 * Dit is een versie van de VlMapWithActions die nog enkele extra functies bevat zoals het zoomen naar een bepaalde extent (of bounding box), het togglen van de layers, en alle functionaliteit omtrent een overzichtskaartje (ol.control.OverviewMap).
 * De view kan in het map opties object bij constructie worden meegegeven of een default view wordt aangemaakt op basis van de projectie.
 */
export declare class VlCustomMap extends VlMapWithActions {
    overviewMapControl: any;
    private projection;
    private view;
    private custom;
    private geoJSONFormat;
    baseLayers: any;
    private maxZoomViewToExtent;
    private overviewMapLayers;
    constructor(options?: MapOptions & {
        customLayers?: {
            baseLayerGroup: LayerGroup;
            overlayGroup: LayerGroup;
            overviewMapLayers?: BaseLayer[];
        };
        projection?: Projection;
        defaultZoom?: boolean;
        maxZoomViewToExtent?: number;
        custom?: any;
        controls?: any[];
        actions?: any[];
        disableEscapeKey?: boolean;
        disableRotation?: boolean;
        disableMouseWheelZoom?: boolean;
        disableKeyboard?: boolean;
    });
    createOverviewMapControl(options: any): void;
    addBaseLayerAndOverlayMapLayer(baseLayer: any, overlayMapLayer: any): void;
    getBaseLayers(): any;
    getOverlayLayers(): BaseLayer[];
    _getOverlayLayersCollection(): Collection<BaseLayer>;
    addOverlayLayer(layer: any): void;
    removeOverlayLayer(layer: BaseLayer): void;
    initializeView(boundingBox?: any, maxZoom?: any): void;
    zoomToExtent(boundingBox: any, maxZoom: any): void;
    zoomViewToExtent(view: any, boundingBox: any, maxZoom: any): void;
    zoomToGeometry(geometry: any, maxZoom: any): void;
    showInfo(info: any, coordinate: any): void;
}
