import { EventsKey } from 'ol/events';
import Draw from 'ol/interaction/Draw';
import Overlay from 'ol/Overlay';
import { VlBaseMapAction } from '../mapaction';
import { Type } from 'ol/geom/Geometry';
export declare class VlDrawAction extends VlBaseMapAction {
    tooltip: Overlay;
    protected measurePointermoveHandler: EventsKey;
    drawInteraction: Draw;
    constructor(layer: any, type: Type, onDraw: any, _options?: {
        style?: any;
        maxPoints?: number;
    });
    activate(): void;
    deactivate(): void;
    _cleanUp(): void;
    _removeTooltip(): void;
    _showMeasureTooltip(feature: any, tooltipElement: any): void;
    _getLengthOfLastSegment(geometry: any): string | 0;
    _getCoordinatesOfLastSegment(geometry: any): any;
    stop(): void;
    pointermove(): void;
}
