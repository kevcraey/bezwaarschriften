var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { webComponent } from '@domg-wc/common';
import { VlMapAction } from '../vl-map-action';
let VlMapLayerAction = class VlMapLayerAction extends VlMapAction {
    static get _observedAttributes() {
        return ['layer'];
    }
    connectedCallback() {
        this._layerChangedCallback();
        return super.connectedCallback();
    }
    get layer() {
        return this._layer;
    }
    set layer(layer) {
        this._layer = layer;
        this._processAction();
    }
    // @ts-expect-error: layer is defined in the parent class
    get _layerElement() {
        return (this._mapElement.querySelector(`[is-layer][name="${this.getAttribute('layer')}"]`) ||
            this.closest('[is-layer]'));
    }
    _layerChangedCallback() {
        if (this._layerElement) {
            this.layer = this._layerElement.layer;
        }
    }
    _processAction(layers) {
        this._mapElement.ready.then(() => {
            if (this._action) {
                this._mapElement.removeAction(this._action);
            }
            if (this.layer) {
                this._action = this._createAction(this.layer);
                super._processAction();
            }
            if (layers) {
                this._action = this._createAction(layers);
                super._processAction();
            }
        });
    }
};
VlMapLayerAction = __decorate([
    webComponent('vl-map-layer-action')
], VlMapLayerAction);
export { VlMapLayerAction };
//# sourceMappingURL=vl-map-layer-action.js.map