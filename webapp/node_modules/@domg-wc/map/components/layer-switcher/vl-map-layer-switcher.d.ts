import { BaseLitElement } from '@domg-wc/common';
import { CSSResult, PropertyDeclarations, TemplateResult } from 'lit';
export declare class VlMapLayerSwitcher extends BaseLitElement {
    private componentTitle;
    private layers;
    private vlMapLayers;
    private mapElement;
    private layerObserver;
    static get styles(): (CSSResult | CSSResult[])[];
    static get properties(): PropertyDeclarations;
    connectedCallback(): Promise<void>;
    disconnectedCallback(): void;
    protected willUpdate(changedProperties: Map<string, unknown>): Promise<void>;
    protected updated(): void;
    protected render(): TemplateResult;
    private layersReady;
    private getVlMapLayers;
    private computeCheckboxAttributes;
    private observeMapLayers;
}
declare global {
    interface HTMLElementTagNameMap {
        'vl-map-layer-switcher': VlMapLayerSwitcher;
    }
}
